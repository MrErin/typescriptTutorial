// this is the simplest way to make this work, but it's not great.
// declare var $: any

// it's better to install a definition file from definitelytyped via npm

$("button").click(function() {
  alert("button was clicked");
});
